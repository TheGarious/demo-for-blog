name: CI Travis Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Travis CI
      resolves: 
      uses: travis-ci/actions@master
      with:
        ${{ secrets.SECRET_TRAVIS_CI }}
